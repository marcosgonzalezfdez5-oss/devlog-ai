[pytest]
; All tests are plain functions - disable class-based collection so pytest
; doesn't try to treat domain models like TestResults/TestCategory as test classes.
python_classes = NoClassBasedTests
