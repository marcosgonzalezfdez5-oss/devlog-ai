# Prompts

Every prompt the Deep Agent uses lives in its own file here, not inline in Python.
Each prompt file states its single objective and the expected output shape. Code
loads a prompt by name; it never constructs or edits prompt text at runtime.

Populated starting in Phase 4 (Deep Agent implementation).
